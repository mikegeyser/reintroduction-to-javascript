import { withBrowserSupport } from '../shared/with-browser-support';

export class ArraysBrowserSupport extends withBrowserSupport('map(), for...of, for...in', '98.2%', {}, "https://caniuse.com/#feat=es5") {}
