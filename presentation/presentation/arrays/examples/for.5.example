
    const numbers = ["one", "two", "three", "four"];
    
            1️              2️           👇🚨
    for(let i = 0; i < numbers.length; i++) {
      2 console.log(numbers[i]); // one, two, three, four
    }