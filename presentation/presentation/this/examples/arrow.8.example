    
    const numbers = [1, 2, 3, 4];

    const exponents = numbers.map((element) => { 
        return 2 ** element;
    });

    const sumOfExponents = exponents.reduce((sum, element) => {
        return sum += element;
    }, 0);

    console.log(sumOfExponents);
    